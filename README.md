1) Build and run the server (Agora project)
2) Build and run the client (ClientAgora project). Pass a string as a parameter. It is required.

Simulation:
Build and run the simulation (Simulation project)
